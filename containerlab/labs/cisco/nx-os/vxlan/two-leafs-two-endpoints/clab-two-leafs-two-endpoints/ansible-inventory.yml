all:
  vars:
    # The generated inventory is assumed to be used from the clab host.
    # Hence no http proxy should be used. Therefore we make sure the http
    # module does not attempt using any global http proxy.
    ansible_httpapi_use_proxy: false
  children:
    cisco_csr1000v:
      vars:
        # ansible_connection: set ansible_connection variable if required
        ansible_user: admin
        ansible_password: admin
      hosts:
        clab-two-leafs-two-endpoints-S1:
          ansible_host: 172.100.100.16
        clab-two-leafs-two-endpoints-S2:
          ansible_host: 172.100.100.17
    cisco_n9kv:
      vars:
        # ansible_connection: set ansible_connection variable if required
        ansible_user: admin
        ansible_password: admin
      hosts:
        clab-two-leafs-two-endpoints-LEAF1:
          ansible_host: 172.100.100.11
        clab-two-leafs-two-endpoints-LEAF2:
          ansible_host: 172.100.100.12
    linux:
      hosts:
        clab-two-leafs-two-endpoints-GRAPHITE:
          ansible_host: 172.100.100.100